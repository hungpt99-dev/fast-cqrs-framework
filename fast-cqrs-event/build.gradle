plugins {
    id 'java-library'
}

description = 'Fast Framework Event - Lightweight event-driven infrastructure'

dependencies {
    // Logging
    implementation 'org.slf4j:slf4j-api'
    
    // Testing
    testImplementation 'org.springframework.boot:spring-boot-starter-test'
}
