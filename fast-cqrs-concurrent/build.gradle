plugins {
    id 'java-library'
}

description = 'Fast Framework Concurrent - Virtual Thread utilities for concurrent execution'

dependencies {
    // Logging
    implementation 'org.slf4j:slf4j-api'
    
    // Testing
    testImplementation 'org.springframework.boot:spring-boot-starter-test'
}
